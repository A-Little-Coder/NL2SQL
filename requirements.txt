# ============================================================================
# NL2SQL 项目依赖配置文件
#
# 使用 conda 创建虚拟环境：
#   conda create -n NL2SQL python=3.10
#   conda activate NL2SQL
#   pip install -r requirements.txt
# ============================================================================

# ============================================================================
# 核心依赖
# ============================================================================

# Hugging Face 数据集
datasets>=2.14.0

# LSH 索引 - 局部敏感哈希库
datasketch>=1.6.0

# LLM 相关 - Qwen API
dashscope>=1.14.0

# Embedding 模型 - BGE-M3
FlagEmbedding>=1.2.0

# 向量数据库 - ChromaDB
chromadb>=0.4.0

# SQL 解析和验证
sqlglot>=20.0.0

# 自然语言处理 - 实体识别
nltk>=3.8.0

# LangSmith 监控
langsmith>=0.1.0

# 数据库连接
# SQLite - Python 内置，无需安装
# MySQL 连接（如果需要）
mysql-connector-python>=8.0.0

# 工具库
python-dotenv>=1.0.0    # 环境变量管理
pyyaml>=6.0             # YAML 配置解析
tqdm>=4.65.0            # 进度条
loguru>=0.7.0           # 日志记录

# 测试
pytest>=7.4.0           # 单元测试框架

# ============================================================================
# 可选依赖（性能优化）
# ============================================================================
# torch>=2.0.0          # PyTorch（BGE-M3 运行需要）
# sentence-transformers>=2.2.0  # 备选 embedding 方案