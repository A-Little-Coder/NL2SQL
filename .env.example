# ============================================================================
# NL2SQL Agent 环境变量配置示例
# ============================================================================
# 复制此文件为 .env 并按需修改：
#   cp .env.example .env
# ============================================================================

# ============================================================================
# LLM API - Qwen
# ============================================================================
QWEN_API_KEY=your_qwen_api_key_here
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
QWEN_MODEL=qwen3.6-plus-2026-04-02

# ============================================================================
# Embedding 模型 - BGE-M3（本地路径或 HuggingFace 模型名）
# ============================================================================
BGE_M3_MODEL_PATH=BAAI/bge-m3

# ============================================================================
# LangSmith 监控
# ============================================================================
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=your_langsmith_api_key_here
LANGCHAIN_PROJECT=NL2SQL

# ============================================================================
# 联网搜索 - Tavily（用于 Clarification Agent）
# ============================================================================
TAVILY_API_KEY=your_tavily_api_key_here

# ============================================================================
# API 服务配置（决策 49：多数据库分池）
# ============================================================================
# DbContextPool 的最大容量（同时缓存多少个 db 的 DbContext）
# 每个 DbContext 占用约 50-100MB（LSH + sqlite 连接）
# BGE-M3 等全局组件不计入池容量
DB_POOL_MAX_SIZE=2

# ============================================================================
# API 流式响应（决策 50：真流式 + LLM 思考链推送）
# ============================================================================
# 是否开启 qwen3 思考链推送（reasoning_content → llm_thinking SSE 事件）
# 关闭时 LLM 仅推送 stage / 业务事件，不推思考过程
LLM_ENABLE_THINKING=true

# SSE 心跳间隔（秒）：连续多久无业务事件时 yield `: heartbeat`
# 用于防止客户端 / 反向代理因 idle 断开 SSE 长连接
SSE_HEARTBEAT_INTERVAL=15

# ============================================================================
# 查询并发闸（change multi-session-concurrency）
# ============================================================================
# 同时在飞的 /query 数上限（全局，跨会话/用户/db 共享，因后端共用单一 API Key）
# 超过则排队（FIFO 公平），排队超时见 QUERY_QUEUE_TIMEOUT
# 依赖"单条查询内 LLM 调用串行"不变量（design D2）：故请求级闸等价 LLM 级闸
QUERY_MAX_CONCURRENCY=4

# 排队超时（秒）：等待槽位超过此值则终止并返回"排队超时，当前服务繁忙，请稍后重试"
QUERY_QUEUE_TIMEOUT=60
